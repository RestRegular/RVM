CALL指令和INVOKE指令的区别
1. [CALL]指令的调用不会主动返回值，[IVOK]指令的调用会主动返回值。
2. 如果[CALL]指令的调用需要返回值，那么需要配合使用[PUT]指令和[SN]返回值内存空间进行返回值操作。
   [IVOK]指令的调用在返回值时使用[RET]指令自动将需要返回的值进行记录，在接受返回值时自动填充。
3. 实际上[IVOK]指令的底层实现逻辑是采用的ID引用方法进行返回值传递。